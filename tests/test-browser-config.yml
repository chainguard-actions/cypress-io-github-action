name: Test browser and config inputs
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: action
        uses: .
        with:
          command: echo "cypress run with browser config"
          browser: chrome
          config: baseUrl=http://localhost:3000
          config-file: ''
          spec: cypress/e2e/**/*.cy.js
          headed: false
          quiet: false
          project: .
          install: false
          runTests: true
      - name: Assert action completed
        run: |
          echo "Action completed successfully"
          echo "PASS"
