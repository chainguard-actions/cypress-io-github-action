name: Test summary-title input
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - id: action
        uses: .
        with:
          install: false
          runTests: false
          summary-title: My Custom Test Summary

      - name: Assert action completed with summary-title input
        run: |
          echo "PASS: action ran with summary-title input"
